<nav class="px-4 py-4 md:px-8">
    <div class="max-w-7xl mx-auto flex items-center justify-between">
        <!-- Logo -->
        <a href="{{ path('app_coin_index') }}" class="flex items-center">
            <img src="/images/logo.png" alt="Cointer" class="h-8 md:h-10">
        </a>

        <!-- Desktop Navigation -->
        <div class="hidden md:flex items-center gap-2">
            <a href="#" class="px-4 py-2 text-white hover:bg-card rounded-lg transition-colors bg-button">Home</a>
            <a href="#" class="px-4 py-2 text-white hover:bg-card rounded-lg transition-colors bg-button">Portfolio</a>
            <a href="#" class="px-4 py-2 text-white hover:bg-card rounded-lg transition-colors bg-button">Profile</a>
            <a href="#" class="px-4 py-2 text-white rounded-lg transition-colors bg-accent">Log out</a>
        </div>

        <!-- Mobile Menu Button -->
        <button id="mobile-menu-btn" class="md:hidden text-white p-2">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
        </button>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-menu" class="hidden md:hidden mt-4 pb-4">
        <div class="flex flex-col gap-2">
            <a href="#" class="px-4 py-2 text-white hover:bg-card rounded-lg transition-colors">Home</a>
            <a href="#" class="px-4 py-2 text-white hover:bg-card rounded-lg transition-colors">Portfolio</a>
            <a href="#" class="px-4 py-2 text-white hover:bg-card rounded-lg transition-colors">Profile</a>
            <a href="#" class="px-4 py-2 text-white rounded-lg transition-colors text-center bg-accent">Log out</a>
        </div>
    </div>
</nav>

<script>
    document.getElementById('mobile-menu-btn').addEventListener('click', function() {
        document.getElementById('mobile-menu').classList.toggle('hidden');
    });
</script>
